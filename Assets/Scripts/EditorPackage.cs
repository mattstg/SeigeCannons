using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class EditorPackage : MonoBehaviour
{
    //Has all the links to editor UI, so that the editor flow can use it
    public EditorIC editorIC;
    public SaveLoadPanel saveLoadPanel;
}
